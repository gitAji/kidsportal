import { GoogleGenAI, Type } from "@google/genai";
import fetch from "node-fetch";

const GEMINI_API_KEY = "dummy_for_testing_if_env_has_it";

async function run() {
  console.log("Checking if we have vertex set-up...");
}
run();
